"""Test-only fixtures, including the reference passthrough module.

Per SPEC decision 1 (REQ-INFRA-CONTRACT-3), the reference passthrough (identity)
module used to establish XDET-TC-000 lives here under `tests/` and is NEVER
placed in the `modules/` tree.
"""
