# SPEC Review Report: SPEC-TIER-001
Iteration: 1/3
Verdict: FAIL
Overall Score: 0.75

Reasoning context ignored per M1 Context Isolation. This audit is based solely on `.moai/specs/SPEC-TIER-001/{spec.md,plan.md,acceptance.md}`, ground-truth docs (`docs/XDET_SWR_spec_v1.2.md`, `docs/XDET_TestSpec_v1.0.md`, `docs/XDET_EVAL_criteria_v1.1.md`, `docs/XDET_measurement_protocol_v1.0.md`, `docs/XDET_FRD_v1.1.md`, `docs/XDET_P1_dev_plan_v1.0.md`, `docs/XDET_RTM_v1.1.md`), and the current codebase (`common/equivalence.py`, `pipeline/orchestrator.py`, `pipeline/sequence.py`, `tests/test_tc_skeletons.py`, `tests/test_contract.py`).

## Must-Pass Results

- [PASS] MP-1 REQ number consistency: This project uses a grouped `REQ-TIER-{GROUP}-{N}` scheme (established convention, also used in SPEC-NDT-001/SPEC-INFRA-001). Verified each group is sequential with no gaps/no duplicates: GATE-1..5 (spec.md:L51-55), EQUIV-1..4 (L59-62), TIMING-1..2 (L66-67), CONTRACT-1..5 (L71-75), VALIDATE-1..4 (L79-82). 20 REQs total, all groups internally sequential.
- [PASS, with caveat] MP-2 EARS format compliance: All 20 REQ-TIER-* entries carry an explicit EARS pattern tag (Event-Driven/Ubiquitous/State-Driven/Unwanted) and use modal "해야 한다"/"않아야 한다" (shall/shall-not) in their primary clause (e.g. spec.md:L53 "WHEN...THEN 시스템은...수용하고...선택해야 한다"; L61 "WHILE...프레임은...보고해야 한다"). **Caveat (D4 below)**: six REQs (GATE-1 L51, EQUIV-1 L59, EQUIV-2 L60, CONTRACT-5 L75, VALIDATE-1 L79, VALIDATE-2 L80) append a second, non-modal declarative sentence after the EARS-compliant clause (e.g. L79 "...XDET-TC-020을 pytest skeleton(skip)에서 실동작 구조 케이스로 전환한다" — present-tense, no "해야 한다"). This is the literal condition MP-2 names as a FAIL trigger ("mixed informal/formal within a single criterion"). I checked SPEC-NDT-001 (status: implemented) for precedent: `REQ-NDT-CONTRACT-2` uses the identical pattern (trailing non-modal sentence after the EARS clause) and that SPEC already passed to "implemented" status under this project's own review process. Given this is a repo-wide, already-accepted convention rather than an author-introduced defect unique to this SPEC, I am not escalating this to an MP-2 FAIL, but I am reporting it as D4 (below) because per M4 I must not silently rationalize a discrepancy — it is a real, repeated formatting weakness that should be tightened.
- [PASS, with caveat] MP-3 YAML frontmatter validity: spec.md:L1-11 has `id: SPEC-TIER-001` (string, matches pattern), `version: 0.1.0` (string), `status: draft` (string), `priority: high` (string), `labels: [T10, tier-gating]` (array). **Caveat**: the date field is literally named `created` (L5), not `created_at` as MP-3's checklist specifies verbatim. Value is a correct ISO date (`2026-07-09`). I verified this is a consistent, repo-wide convention — SPEC-NDT-001:L5 and SPEC-INFRA-001:L5 both use `created` identically, and both of those SPECs are already `status: implemented`. Since the field is present, correctly typed, and semantically equivalent, and the deviation is systemic (not unique to this SPEC), I am not treating this as an MP-3 FAIL, but flag it (D5) as a naming-convention gap against the literal audit rubric.
- [N/A] MP-4 Section 22 language neutrality: SPEC-TIER-001 is scoped to a single-domain X-ray FPD hardware-tier gating/equivalence-diff framework, not multi-language LSP tooling. No language-specific tool names appear. N/A: single-domain SPEC.

## Category Scores (0.0-1.0, rubric-anchored)

| Dimension | Score | Rubric Band | Evidence |
|-----------|-------|-------------|----------|
| Clarity | 0.75 | 0.75 — minor ambiguity in one requirement a reasonable engineer would resolve differently | acceptance.md:L49-52 (EC-3) permits two divergent behaviors ("명시 오류 또는 정의된 보수적 기본 처리") for invalid/missing capability descriptors without disambiguating which one implementers must build — this directly contradicts the SPEC's own repeated "단일 결정론 경로, 비결정적 택일 없음" principle stated for the sibling case in spec.md:L54 (GATE-4) and acceptance.md:L20 (Scenario 3). |
| Completeness | 0.75 | 0.75 — one non-critical section/field gap; frontmatter otherwise complete | All narrative sections present across the 3-file split (HISTORY spec.md:L26-34, WHY/WHAT via intro+Environment L15-46, REQUIREMENTS L47-82, Exclusions L84-93 with 7 specific, non-vague entries, ACCEPTANCE CRITERIA via acceptance.md). Docked from 1.0 for the literal `created`-vs-`created_at` frontmatter field-name gap (MP-3 caveat above) and the REQ-TIER-CONTRACT-3 traceability gap (see Traceability row) — both are real, if minor, completeness shortfalls. |
| Testability | 0.75 | 0.75 — one AC not precisely binary-testable | Scenarios 1-6 and their DoD lines are precise and machine-checkable (`structurally_equal=True/False`, `max_pixel_abs_diff==0`/`>0`, explicit error raised, timing record fields) — e.g. acceptance.md:L25 "structurally_equal=True 및 max_pixel_abs_diff==0". EC-3 (acceptance.md:L49-52) is the one exception: "명시 오류 또는 정의된 보수적 기본 처리(...)로 처리한다" gives a tester no single pass/fail bar, and "정의된 보수적 기본 처리" is itself a weasel phrase — "defined" without the definition being given anywhere in this SPEC. |
| Traceability | 0.75 | 0.75 — one REQ uncovered / indirectly mapped | REQ-TIER-CONTRACT-3 (spec.md:L73, numeric-threshold-not-hardcoded) is never cited by ID anywhere in acceptance.md. Verified via grep — acceptance.md only cites CONTRACT-1 (L64-65), CONTRACT-2 (L66), CONTRACT-4 (L73 of acceptance.md), CONTRACT-5 (L67 of acceptance.md); CONTRACT-3 is absent. Its content is split and indirectly covered by DoD line 68 (GATE-1·2·5, tier-threshold-not-hardcoded part only) and DoD line 74 (VALIDATE-3, numeric-gate-deferral part only), but the REQ ID itself is orphaned from explicit AC traceability. All other 19 REQ IDs are explicitly cited in acceptance.md Scenarios/DoD, and every acceptance.md REQ citation resolves to a real REQ ID in spec.md (no reverse-orphans found). |

## Ground-Truth Cross-Verification (independent re-derivation)

- **SWR-1301/1302/1303 text** (docs/XDET_SWR_spec_v1.2.md:145-147) verified verbatim: SWR-1301 = "티어 판정: CPU 코어·AVX 지원, GPU 모델·VRAM(임계 TBD: P2 벤치마크), 판정 결과·근거 로그. 사용자 강제 하향 허용, 강제 상향 금지" — matches spec.md's REQ-TIER-GATE-1/3/4 exactly, including the "임계 TBD: P2 벤치마크" quote (spec.md:L51).
- **Tier semantics re-derived independently (not trusted from SPEC's own conclusion)**: SWR-1301 defines tier purely by hardware capability inputs (CPU cores/AVX/GPU/VRAM); XDET-EV-401 (docs/XDET_EVAL_criteria_v1.1.md:137-142) frames tiers as *execution paths* ("Tier 2 전체 파이프라인" vs "Tier 1 결정론적 파이프라인") with processing-time targets attached to the path, not to an achieved-quality classification. XDET-FR-C012 (docs/XDET_FRD_v1.1.md:20) independently confirms: "실행 HW 자동 감지(...) → 티어 판정 → 기능 gating." None of SWR-1301, EV-401, or FR-C012 map tier to EV min/typ/max image-quality grades. **Independent conclusion: SPEC's Decision 3 (tier = hardware execution-path capability, not an EV quality grade) is correct and well-grounded — PROCEED, not a defect.**
- **Appendix A-2 grade citations verified**: "1302(±1 LSB) = [P]" and "000 계열 = [C]" both match docs/XDET_SWR_spec_v1.2.md:162-163 verbatim. SPEC does not fabricate a grade tag for SWR-1301's threshold (correctly sources it from SWR-1301 body text, not the grade table, since 1301 is absent from Appendix A/A-2). No grade-fidelity defects found.
- **XDET-TC-020/021 exact wording verified against TestSpec** (docs/XDET_TestSpec_v1.0.md:27-28) and against the live skip-skeleton (tests/test_tc_skeletons.py:67-68): the skeleton's descriptions — `"tier gating structure (T10)"` and `"equivalence numeric gate: bit-identical / +/-1 LSB (P2)"` — are an exact character-for-character match to spec.md:L21's claimed skeleton text. Both are still `pytest.skip(...)`-gated (test_tc_skeletons.py:74). Confirmed accurate.
- **EV-401/EV-402 body verified** (docs/XDET_EVAL_criteria_v1.1.md:137-148): Tier 2 ≤3s/≤1s/≤0.3s, Tier 1 ≤5s/≤2s/≤1s, EV-402 "진단 영향 기능 결과 차: 정의된 허용오차 내 min / bit 동일 typ / 동일 max" — matches spec.md:L20 exactly.
- **Cross-doc ID chain verified** (WP12/FR-C012/FR-C013/VV-012/TC-020/TC-021/MR-007/MR-010): docs/XDET_FRD_v1.1.md:20-21, docs/XDET_P1_dev_plan_v1.0.md:20, docs/XDET_RTM_v1.1.md:14-15 all cross-reference consistently, and docs/XDET_P1_dev_plan_v1.0.md:20 itself already says "WP12 | 티어 gating + 결과 동일성 프레임 | PR-CORE-007/008 | EV-401/402 (구조만, 수치는 P2)" — i.e. the "structure-only, numeric-P2" framing is not the SPEC author's own invention; it is sourced directly from the dev-plan document. Strong positive evidence against over-promised DoD.
- **common/equivalence.py verified** (Read in full): `diff_frames(a, b) -> EquivalenceDiff` and `EquivalenceDiff(pixel_equal, masks_equal, noise_equal, max_pixel_abs_diff, structurally_equal: property)` exist exactly as SPEC claims. Docstring explicitly states "T0 scope: STRUCTURE ONLY. Numeric thresholds (...) are deferred to P2" and "T0 asserts NO threshold on it (P2 owns the numeric gate)" — matches SPEC's "structure only, numeric P2" claim word-for-word in intent.
- **"T10 is the first real consumer" claim checked via grep**: only `tests/test_contract.py:16,50,54,59` calls `diff_frames`/`EquivalenceDiff` today, and that file is T0's own infrastructure self-test of the hook (test_equivalence_hook_structural_diff), not a downstream work-package consumer. No other module/tests/pipeline file invokes `diff_frames`. This confirms the SPEC's claim (spec.md:L30, L40, L75) that T10 would be the first *downstream* functional consumer of the CI-4 hook — verified accurate, not a defect.
- **pipeline/orchestrator.py CANONICAL_ORDER verified**: contains `offset, gain, defect, lag, line_noise, saturation, geometry, grid, virtual_grid, denoise, mse, window, post` — no "tier" stage present anywhere. `pipeline/tier.py` does not yet exist (Glob confirms; correctly marked `[신규]` in plan.md — prospective, not yet built). This independently confirms Decision 1's "no processing stage, T0 surface unchanged" placement claim.
- **pipeline/sequence.py precedent verified**: it wraps `run_pipeline` without changing its signature, `CANONICAL_ORDER`, or the calibration gate ("`run_pipeline`, `CANONICAL_ORDER`, the entry gate, and the process contract are UNCHANGED (additive)" — sequence.py:L19-20). This is a genuine, already-implemented precedent for the additive-wrapper pattern the SPEC proposes for `pipeline/tier.py`. **Placement judgment: PROCEED — the additive-wrapper claim is code-verified, not merely asserted.**
- **SPEC-NDT-001 (T9) precedent verified**: its HISTORY entry (SPEC-NDT-001/spec.md:28) explicitly states "T0 표면(CANONICAL_ORDER/CalibKind) 무변경 확인(계약 테스트+lint-imports)" for an already-`implemented` SPEC, confirming the "T0-surface-invariant" pattern this SPEC claims to follow (T9 precedent) is real and previously validated, not a hypothetical analogy.
- **REQ-INFRA-CI-4 verified** in SPEC-INFRA-001/spec.md:84 — text matches SPEC-TIER-001's characterization exactly ("등가성 diff 유틸 훅... 수치 임계(±1 LSB/bit-동일)는 P2로 이연하며 T0는 훅 구조만 제공한다").
- **No GPU/Tier-2/CUDA code exists anywhere in the repo** (grep confirmed 0 matches) — independently confirms the Environment/Assumptions claim (spec.md:L39) that P1 has exactly one real implementation (the deterministic CPU float golden model) and no second implementation to bit-compare against yet.

## Defects Found

D1. acceptance.md:L74 (and absence throughout acceptance.md) — REQ-TIER-CONTRACT-3 is never cited by its REQ ID in acceptance.md's Scenario headers or DoD checklist. Its content is split across DoD line 68 (tier-threshold-not-hardcoded, tied to GATE-1·2·5) and DoD line 74 (bit-identical/±1 LSB/timing deferral, tied to VALIDATE-3), but the ID "CONTRACT-3" itself is orphaned from explicit traceability. — Severity: minor (content-covered, ID-untraced)

D2. spec.md:L54-55 (GATE-5), L71-72 (CONTRACT-1), L72 (CONTRACT-2), L75 (CONTRACT-5), L59-60 (EQUIV-1) — Implementation-detail leakage into WHAT-level requirements: concrete module/class/function/file identifiers (`pipeline/tier.py`, `common/equivalence.py`, `CANONICAL_ORDER`, `CalibKind`, `_KIND_BY_STAGE`, `diff_frames`, `EquivalenceDiff`, `PipelineDefinition`, `run_pipeline`) are embedded directly in normative REQ text rather than kept to plan.md's HOW section. This is precedented by an already-implemented sibling SPEC (SPEC-NDT-001's REQ-NDT-CONTRACT-2 does the same), so it reflects an established (if rubric-noncompliant) project convention for "T0-surface-invariance" contracts rather than a novel authoring error — reported per M4/RQ-3/RQ-4 regardless of precedent. — Severity: minor (systemic, precedented, not blocking on its own)

D3. acceptance.md:L49-52 (EC-3, tied to REQ-TIER-GATE-1) — Disjunctive, non-binary-testable requirement: "명시 오류 또는 정의된 보수적 기본 처리(...)로 처리한다" permits two materially different, observably distinguishable implementer choices without disambiguating which is required. This also contradicts the SPEC's own stated design principle of a single deterministic path with no non-deterministic choice (explicitly asserted for the sibling forced-upgrade case at spec.md:L54 and acceptance.md:L20). "정의된 보수적 기본 처리" additionally uses "정의된" ("defined") without that definition existing anywhere in this SPEC — a weasel-adjacent phrase. — Severity: major (this is the one clause in the document that a tester genuinely cannot grade PASS/FAIL against a single bar, and it self-contradicts a principle asserted elsewhere in the same document)

D4. spec.md:L51 (GATE-1), L59 (EQUIV-1), L60 (EQUIV-2), L75 (CONTRACT-5), L79 (VALIDATE-1), L80 (VALIDATE-2) — Six EARS-tagged REQ entries append a second, non-modal, present-tense declarative sentence after the properly-EARS-formatted primary clause (e.g. L79: "...XDET-TC-020을 pytest skeleton(skip)에서 실동작 구조 케이스로 전환한다" — no "해야 한다"). This literally matches MP-2's named failure mode ("mixed informal/formal within a single criterion"). Not escalated to an MP-2 must-pass FAIL because the identical pattern appears in the already-implemented SPEC-NDT-001 (REQ-NDT-CONTRACT-2) and is therefore an established, previously-tolerated project convention — but it remains a real formatting weakness worth tightening (split the trailing rationale into a parenthetical citation or a separate non-normative sentence clearly marked as rationale). — Severity: minor (systemic, precedented)

D5. spec.md:L5, plan.md:L6 — YAML frontmatter date field is named `created`, not the MP-3-specified `created_at`. Value and format are correct (ISO date `2026-07-09`); the deviation is a naming-convention mismatch, verified consistent across the whole repo (SPEC-NDT-001:L5, SPEC-INFRA-001:L5 use the identical field name and both those SPECs are already `status: implemented`). Not escalated to an MP-3 must-pass FAIL given repo-wide, pre-existing consistency, but reported per M4 since it is a literal deviation from the audit checklist's named field. — Severity: minor (systemic, precedented, cosmetic)

D6. spec.md:L52 (REQ-TIER-GATE-2) — Conjunctive/compound Ubiquitous requirement bundles two distinct assertions ("실행 경로를 결정해야 하며... EV 등급으로 분류하지 않아야 한다") under a single REQ ID rather than splitting into two atomic, independently-traceable requirements. Both assertions are individually precise and testable, so this is an atomicity/decomposition nitpick rather than a testability failure. — Severity: minor

## Chain-of-Verification Pass

Second-look findings: re-read all 20 REQ entries individually (not skimmed after the first few) to check EARS-tag correctness and HOW-leakage — confirmed D2 and D4 populations are complete (no additional undiscovered instances found on re-read: GATE-2/3/4, EQUIV-3/4, TIMING-1/2, CONTRACT-3/4, VALIDATE-3/4 do NOT exhibit the D2/D4 patterns and were correctly excluded). Re-verified REQ numbering end-to-end across all 5 groups (not spot-checked) — confirmed no gaps/duplicates in any group. Re-verified traceability for all 20 REQ IDs against acceptance.md (not sampled) — confirmed the single CONTRACT-3 gap (D1) and confirmed zero reverse-orphans (every acceptance.md REQ citation resolves to a real spec.md REQ ID). Re-checked the Exclusions section (spec.md:L84-93) for specificity, not just presence — all 7 entries are concrete and SWR/CLAUDE.md-anchored, none are vague filler. Re-scanned for cross-requirement contradictions (not just within-requirement) — found the D3 contradiction between EC-3's disjunctive handling and GATE-4/Scenario-3's "single deterministic path" principle; no other contradictions found (TIMING-2's "속도 최적화 금지" is consistent with CLAUDE.md's project-wide policy, verified). No additional defects surfaced beyond D1-D6 in this second pass.

## Recommendation

FAIL — overall score 0.75 is below the required 0.80 threshold, driven by four real (if individually modest) defects across all four category dimensions. No must-pass firewall was independently triggered, so this is a scoring-threshold FAIL rather than a structural rejection. Fix instructions for manager-spec:

1. **Resolve D3 (major) first**: acceptance.md EC-3 (L49-52) — pick a single deterministic behavior for invalid/missing capability descriptors (explicit error is the more consistent choice given GATE-4's precedent of "단일 결정론 경로, 비결정적 택일 없음") and remove the "또는" alternative. If a conservative-default path is genuinely intended, define its exact output structure in this SPEC (not "정의된" pointing nowhere) and state under what condition it applies vs. the error path — the two cannot both be valid outcomes of the same input class.
2. **Fix D1**: add an explicit `CONTRACT-3` citation to acceptance.md's DoD checklist (e.g., append `, CONTRACT-3` to DoD line 68 and/or line 74, whichever half of its content each line covers) so every REQ ID in spec.md has at least one direct ID-level citation in acceptance.md.
3. **Consider D2**: either accept the established project convention of naming architectural-surface identifiers directly in REQ text (as SPEC-NDT-001 already does) and note this explicitly as a deliberate project-level EARS extension in a short addendum, or move the concrete identifiers (`pipeline/tier.py`, `CANONICAL_ORDER`, `CalibKind`, `_KIND_BY_STAGE`, `diff_frames`, `EquivalenceDiff`) into plan.md's HOW section and keep spec.md's REQ text scoped to "the fixed processing-stage order" / "the calibration-kind registry" / "the equivalence-diff hook" in prose, with identifiers only in parenthetical citations.
4. **Consider D4**: for GATE-1, EQUIV-1, EQUIV-2, CONTRACT-5, VALIDATE-1, VALIDATE-2 — either reword the trailing sentence with modal "해야 한다"/"않아야 한다" so the whole entry is EARS-compliant, or explicitly demote it to a parenthetical rationale (in `(...)`) so it reads unambiguously as non-normative context rather than a second, informally-phrased requirement clause.
5. **Optional/cosmetic (D5, D6)**: no action required unless the project decides to standardize frontmatter on `created_at` project-wide, or to split REQ-TIER-GATE-2 into two atomic REQs (e.g., -2a path-selection, -2b not-an-EV-grade) for stricter one-assertion-per-REQ hygiene.

None of these fixes require re-deriving the tier/placement architecture — both were independently re-verified against SWR-1301/1302, EV-401/402, FR-C012/C013, and the current codebase and found correct (see Ground-Truth Cross-Verification section). Re-audit should focus on D1/D3 (the two most load-bearing items) plus a targeted regression check that D2/D4/D5/D6 were not worsened.
