# .NET build artifacts (SPEC-XSEAM-001 P1.5 fidelity core)
bin/
obj/
*.log

# UI-smoke screenshots (SPEC-REALDATA-001 real-image tab): run artifacts, not source.
_screens/

# Rider / VS
.vs/
.idea/

# Re-include the C# solution file: the repo-root .gitignore excludes *.sln
# (Python-project default), but the xdet-console solution MUST be tracked.
!*.sln
